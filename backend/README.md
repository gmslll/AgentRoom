# Backend

Backend services for AgentRoom, including HTTP APIs, realtime chat, file
handling, persistence, and terminal/AI integrations.

This directory is owned by the backend developer and should contain its own:

- dependency manifest and lockfile
- application source code
- database migrations
- tests
- service configuration
- `.env.example`

Publish language-neutral API and realtime message definitions to
`../shared/contracts/`.
