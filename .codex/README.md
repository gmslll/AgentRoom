# Codex project configuration

Codex repository instructions live in the root and nested `AGENTS.md` files,
which Codex discovers automatically.

Use this `.codex/` directory only for future repository-scoped Codex
configuration, such as `config.toml`, hooks, or other supported project
settings. Do not duplicate `AGENTS.md` rules here.
