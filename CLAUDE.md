@AGENTS.md

# Claude Code notes

- Path-specific project rules live in `.claude/rules/`.
- Use `/memory` when diagnosing whether these instructions were loaded.
- Keep this file short. Put shared repository guidance in `AGENTS.md` so Codex
  and Claude Code receive the same source of truth.
