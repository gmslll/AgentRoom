# Frontend

Web client for AgentRoom.

This directory is owned by the frontend developer and should contain its own:

- package manifest and lockfile
- source code and public assets
- tests
- build and lint configuration
- `.env.example`

Consume API and realtime message definitions from `../shared/contracts/`.
